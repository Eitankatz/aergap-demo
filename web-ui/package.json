{
  "name": "aergap-demo-web-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only client for the Aergap demo: Hermes chat (left) + live policy outcome & audit record (right), via the hermes dashboard /api/events WebSocket and /api/sessions/{id}/messages.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
